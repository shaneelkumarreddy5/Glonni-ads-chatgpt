"use client";

import { AlertTriangle, ArrowUpRight, CheckCircle2, Clock3, Inbox, Link2, Mail, Search, ShieldCheck, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";

type Action = (message: string) => void;

const supportSubagents = [
  { name: "Support Intake & Classification", duty: "Classifies language, intent, urgency, duplicates and assigns the correct queue.", load: 18, state: "Ready" },
  { name: "Missing Rewards Support", duty: "Checks click, conversion, postback and provider evidence before recommending a resolution.", load: 7, state: "Ready" },
  { name: "Withdrawals Support", duty: "Explains withdrawal state and escalates payment failures without releasing funds.", load: 4, state: "Protected" },
  { name: "KYC & Account Support", duty: "Guides verification, recovery and restrictions without overriding compliance decisions.", load: 6, state: "Protected" },
  { name: "Escalation & Follow-up", duty: "Tracks SLA deadlines and coordinates Fraud, Payments, Compliance and Technical teams.", load: 5, state: "Ready" },
  { name: "Support Quality", duty: "Samples conversations, scores accuracy and proposes instruction improvements.", load: 12, state: "Review" },
];

const tickets = [
  { id: "SUP-8421", customer: "Aarav Mehta", subject: "Game milestone reward missing", category: "Missing rewards", owner: "Missing Rewards Support", priority: "High", status: "Investigating", sla: "38 min left" },
  { id: "SUP-8420", customer: "Priya Reddy", subject: "Withdrawal remains pending", category: "Withdrawals", owner: "Withdrawals Support", priority: "High", status: "Escalated", sla: "12 min left" },
  { id: "SUP-8419", customer: "Meera Shah", subject: "PAN name differs from profile", category: "KYC & account", owner: "KYC & Account Support", priority: "Medium", status: "Waiting", sla: "1h 14m left" },
  { id: "SUP-8418", customer: "Vikram Singh", subject: "Cannot sign in after device change", category: "Account access", owner: "KYC & Account Support", priority: "Critical", status: "New", sla: "Breached 6 min" },
  { id: "SUP-8417", customer: "Ananya Rao", subject: "Survey completion not credited", category: "Missing rewards", owner: "Missing Rewards Support", priority: "Low", status: "Resolved", sla: "Met" },
];

const tone = (value: string) => value === "Critical" || value.includes("Breached") ? "bg-rose-50 text-rose-700" : value === "High" || value === "Escalated" || value === "Review" ? "bg-amber-50 text-amber-700" : value === "Ready" || value === "Resolved" || value === "Met" ? "bg-emerald-50 text-emerald-700" : value === "Protected" ? "bg-violet-50 text-violet-700" : "bg-slate-100 text-slate-600";

export function SupportTeamLeadOverview({ onOpen }: { onOpen: (tab: string) => void }) {
  return <div className="space-y-5">
    <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Open tickets","40","5 need attention",Inbox],["SLA at risk","3","1 already breached",Clock3],["Resolved today","126","92% first-contact",CheckCircle2],["Customer satisfaction","4.7/5","218 responses",Sparkles]].map(([label,value,note,Icon], index) => <article key={label as string} className="rounded-2xl border bg-white p-5 shadow-sm"><span className={`grid h-10 w-10 place-items-center rounded-xl ${index === 1 ? "bg-rose-50 text-rose-600" : "bg-violet-50 text-violet-600"}`}><Icon size={19}/></span><p className="mt-4 text-xs font-semibold text-slate-500">{label as string}</p><p className="mt-1 text-2xl font-bold">{value as string}</p><p className="mt-1 text-[10px] text-slate-400">{note as string}</p></article>)}</section>
    <section className="grid gap-5 xl:grid-cols-[1.3fr_.7fr]"><article className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex items-center border-b p-5"><div><h2 className="font-bold">Subagent workload</h2><p className="mt-1 text-xs text-slate-500">Live operating responsibilities and protected boundaries.</p></div><button onClick={() => onOpen("Subagents")} className="ml-auto text-xs font-bold text-violet-700">Manage team →</button></div><div className="grid gap-3 p-4 md:grid-cols-2">{supportSubagents.map((agent, index) => <article key={agent.name} className="rounded-xl border p-4"><div className="flex items-start gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-violet-50 text-[10px] font-bold text-violet-700">{String(index + 1).padStart(2,"0")}</span><div className="min-w-0"><b className="block text-xs">{agent.name}</b><p className="mt-1 text-[10px] text-slate-400">{agent.load} items in current workload</p></div><span className={`ml-auto rounded-full px-2 py-1 text-[9px] font-bold ${tone(agent.state)}`}>{agent.state}</span></div></article>)}</div></article>
      <div className="space-y-5"><article className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center"><h2 className="font-bold">Needs attention</h2><span className="ml-auto rounded-full bg-rose-50 px-2 py-1 text-[9px] font-bold text-rose-700">3 ITEMS</span></div><div className="mt-4 space-y-3">{[["SUP-8418","Login issue breached SLA"],["SUP-8420","Payment escalation waiting"],["QA-112","Reply failed accuracy check"]].map(([id,label]) => <button key={id} onClick={() => onOpen("Support Tickets")} className="flex w-full items-center gap-3 rounded-xl border p-3 text-left"><AlertTriangle size={16} className="text-rose-600"/><div><b className="block text-xs">{label}</b><p className="text-[10px] text-slate-400">{id}</p></div><ArrowUpRight size={14} className="ml-auto text-violet-600"/></button>)}</div></article><article className="rounded-2xl border border-violet-200 bg-violet-50 p-5"><ShieldCheck size={19} className="text-violet-700"/><h2 className="mt-3 font-bold text-violet-950">Human decision boundary</h2><p className="mt-2 text-xs leading-5 text-violet-900">The team may investigate, draft and recommend. Payouts, wallet adjustments, KYC or fraud overrides and policy exceptions require an authorized human approval.</p></article></div>
    </section>
  </div>;
}

export function SupportSubagents({ onInstruct }: { onInstruct: (name: string) => void }) {
  return <section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-bold">Support specialist subagents</h2><p className="mt-1 text-xs text-slate-500">Each specialist has a narrow assignment, explicit escalation path and direct instruction target.</p></div><div className="grid gap-4 p-5 lg:grid-cols-2">{supportSubagents.map((agent,index) => <article key={agent.name} className="rounded-2xl border p-5"><div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-50 text-xs font-bold text-violet-700">{String(index + 1).padStart(2,"0")}</span><div><h3 className="text-sm font-bold">{agent.name}</h3><p className="mt-2 text-xs leading-5 text-slate-500">{agent.duty}</p></div></div><div className="mt-4 flex items-center border-t pt-4"><span className={`rounded-full px-2 py-1 text-[9px] font-bold ${tone(agent.state)}`}>{agent.state}</span><span className="ml-2 text-[10px] text-slate-400">{agent.load} workload items</span><button onClick={() => onInstruct(agent.name)} className="ml-auto h-9 rounded-xl border border-violet-200 px-3 text-[10px] font-bold text-violet-700">Chat & instruct</button></div></article>)}</div></section>;
}

export function SupportTickets({ action }: { action: Action }) {
  const [query,setQuery] = useState("");
  const [filter,setFilter] = useState("All");
  const [selected,setSelected] = useState<(typeof tickets)[number] | null>(null);
  const visible = useMemo(() => tickets.filter((ticket) => `${ticket.id} ${ticket.customer} ${ticket.subject} ${ticket.category}`.toLowerCase().includes(query.toLowerCase()) && (filter === "All" || ticket.status === filter)), [query,filter]);
  return <><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex flex-col gap-3 border-b p-5 lg:flex-row lg:items-center"><div><h2 className="font-bold">Support Tickets</h2><p className="mt-1 text-xs text-slate-500">Unified queue with assignment, SLA, escalation and complete customer history.</p></div><div className="flex flex-1 gap-2 lg:ml-auto lg:max-w-2xl"><label className="flex h-10 flex-1 items-center gap-2 rounded-xl border px-3"><Search size={14}/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search tickets" className="w-full text-xs outline-none"/></label><select value={filter} onChange={(event) => setFilter(event.target.value)} className="h-10 rounded-xl border bg-white px-3 text-xs"><option>All</option>{["New","Investigating","Waiting","Escalated","Resolved"].map((item) => <option key={item}>{item}</option>)}</select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase text-slate-400"><tr>{["Ticket / customer","Issue","Assigned specialist","Priority","Status","SLA","Action"].map((heading) => <th key={heading} className="px-5 py-3">{heading}</th>)}</tr></thead><tbody>{visible.map((ticket) => <tr key={ticket.id} className="border-t"><td className="px-5 py-4"><b>{ticket.customer}</b><p className="font-mono text-[10px] text-violet-600">{ticket.id}</p></td><td className="px-5 py-4"><b>{ticket.subject}</b><p className="text-[10px] text-slate-400">{ticket.category}</p></td><td className="px-5 py-4">{ticket.owner}</td><td className="px-5 py-4"><span className={`rounded-full px-2 py-1 text-[9px] font-bold ${tone(ticket.priority)}`}>{ticket.priority}</span></td><td className="px-5 py-4"><span className={`rounded-full px-2 py-1 text-[9px] font-bold ${tone(ticket.status)}`}>{ticket.status}</span></td><td className="px-5 py-4"><span className={ticket.sla.includes("Breached") ? "font-bold text-rose-600" : ""}>{ticket.sla}</span></td><td className="px-5 py-4"><button onClick={() => setSelected(ticket)} className="font-bold text-violet-700">Open →</button></td></tr>)}</tbody></table></div></section>{selected && <div className="fixed inset-0 z-[120] flex justify-end bg-slate-950/45" onMouseDown={(event) => event.target === event.currentTarget && setSelected(null)}><aside className="h-full w-full max-w-xl overflow-y-auto bg-[#f7f8fc] p-5 shadow-2xl"><div className="flex items-center"><div><p className="font-mono text-[10px] font-bold text-violet-600">{selected.id}</p><h2 className="text-lg font-bold">{selected.subject}</h2></div><button onClick={() => setSelected(null)} className="ml-auto h-10 rounded-xl border px-4 text-xs font-bold">Close</button></div><section className="mt-5 rounded-2xl border bg-white p-5"><h3 className="font-bold">Customer and assignment</h3><div className="mt-4 grid grid-cols-2 gap-3">{[["Customer",selected.customer],["Category",selected.category],["Owner",selected.owner],["SLA",selected.sla]].map(([label,value]) => <div key={label} className="rounded-xl bg-slate-50 p-3"><p className="text-[10px] text-slate-400">{label}</p><b className="text-xs">{value}</b></div>)}</div></section><section className="mt-4 rounded-2xl border bg-white p-5"><h3 className="font-bold">Conversation timeline</h3><div className="mt-4 space-y-3"><div className="rounded-xl bg-slate-100 p-4 text-xs">Customer reported the issue and supplied the minimum required reference.</div><div className="ml-8 rounded-xl bg-violet-50 p-4 text-xs text-violet-900">{selected.owner} classified the case and prepared an evidence check.</div></div><textarea placeholder="Internal note — never visible to the customer" className="mt-4 min-h-24 w-full rounded-xl border p-3 text-xs outline-none"/><div className="mt-3 flex flex-wrap gap-2"><button onClick={() => action(`${selected.id} draft reply prepared`)} className="h-9 rounded-xl bg-violet-600 px-3 text-xs font-bold text-white">Prepare reply</button><button onClick={() => action(`${selected.id} escalated for protected review`)} className="h-9 rounded-xl border px-3 text-xs font-bold text-rose-700">Escalate</button></div></section></aside></div>}</>;
}

export function SupportTasks() { return <SupportList title="Support Tasks" subtitle="Manual, automated and recurring assignments with deadlines and evidence." items={[["Recover breached SLA ticket","Escalation & Follow-up","Critical · due now"],["Review missing reward evidence","Missing Rewards Support","High · 38 min"],["Sample 20 resolved conversations","Support Quality","Normal · today"],["Follow up on payment escalation","Withdrawals Support","High · 12 min"]]}/>; }
export function SupportReports() { return <SupportList title="Support Reports" subtitle="Operational reports prepared for the Support Lead and Chief Operations Agent." items={[["Daily support summary","Today 18:00 IST","Scheduled"],["Weekly performance report","Monday 09:00 IST","Scheduled"],["Missing rewards analysis","7 open cases","Ready"],["SLA and escalation report","3 at risk","Needs attention"],["Subagent quality report","96.8% pass rate","Ready"]]}/>; }
export function SupportApprovals({ action }: { action: Action }) { return <SupportList title="Support Approvals" subtitle="Evidence and recommendation are prepared here; execution remains human-controlled." action={action} items={[["Manual reward adjustment","SUP-8421 · ₹120 recommendation","CEO / authorized finance"],["Withdrawal exception","SUP-8420 · no fund release","Payments approval"],["Sensitive account recovery","SUP-8418 · device changed","Security approval"],["Policy exception","No active request","CEO approval"]]}/>; }
export function SupportAutomations({ action }: { action: Action }) { return <SupportList title="Support Automations" subtitle="Schedules remain supervised until their connections and approval rules are verified." action={action} items={[["Ticket classification and routing","On every new ticket","Configured"],["SLA risk reminder","Every 15 minutes","Paused"],["Overdue escalation","At SLA breach","Configured"],["Customer follow-up","After 24 hours waiting","Draft"],["Daily Support Lead report","18:00 IST","Draft"],["Weekly Chief Operations report","Monday 09:00 IST","Draft"]]}/>; }

export function SupportConnections({ action }: { action: Action }) {
  return <div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><Mail className="text-violet-600"/><div><h2 className="font-bold">Email Connections</h2><p className="text-xs text-slate-500">Convert incoming support mail into threaded tickets.</p></div></div><div className="mt-5 space-y-3">{[["Support inbox","Not connected","OAuth or provider API"],["Reply threading","Waiting","Requires inbox connection"],["Approved signature","Configured","Glonni Support Team"],["Delivery callbacks","Waiting","Provider webhook"]].map(([label,state,note]) => <div key={label} className="rounded-xl border p-4"><div className="flex items-center"><b className="text-xs">{label}</b><span className={`ml-auto rounded-full px-2 py-1 text-[9px] font-bold ${tone(state)}`}>{state}</span></div><p className="mt-1 text-[10px] text-slate-400">{note}</p></div>)}</div><button onClick={() => action("Email connection setup requires your provider authorization")} className="mt-4 h-10 w-full rounded-xl bg-violet-600 text-xs font-bold text-white">Connect support email</button></section><section className="space-y-5"><article className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Additional channels</h2><div className="mt-4 space-y-3">{[["In-app support","Application API","Ready for integration"],["Website contact form","Secure webhook","Ready for integration"],["Messaging provider","Optional future connection","Not connected"]].map(([label,type,state]) => <div key={label} className="flex items-center gap-3 rounded-xl border p-4"><Link2 size={16} className="text-violet-600"/><div><b className="text-xs">{label}</b><p className="text-[10px] text-slate-400">{type}</p></div><span className="ml-auto text-[9px] font-bold text-slate-500">{state}</span></div>)}</div></article><article className="rounded-2xl border border-amber-200 bg-amber-50 p-5"><ShieldCheck size={18} className="text-amber-700"/><h2 className="mt-3 font-bold text-amber-950">Credentials stay protected</h2><p className="mt-2 text-xs leading-5 text-amber-900">Passwords and secret API keys are never saved in browser fields or ticket records. Connections use provider authorization or server-side encrypted credentials.</p></article></section></div>;
}

export function SupportSettings() { return <div className="grid gap-5 lg:grid-cols-2"><SupportList title="Operating Settings" subtitle="Department-level service targets." items={[["Operating hours","24/7 intake · staffed escalation 08:00–22:00 IST","Configured"],["First response target","Critical 15m · High 1h · Normal 4h","Configured"],["Resolution target","Category-specific SLA","Configured"],["Supported languages","English, Hindi, Telugu","Review"]]}/><SupportList title="Safety & Permissions" subtitle="Non-negotiable support boundaries." items={[["Wallet adjustments","Human approval required","Protected"],["Withdrawal release","Never permitted","Protected"],["KYC or fraud override","Never permitted","Protected"],["Sensitive data","Masked by default","Protected"],["Emergency pause","Owner and Security Admin","Available"]]}/></div>; }

export function SupportActivityHistory() { return <SupportList title="Support Activity History" subtitle="Read-only evidence for messages, assignments, escalations, approvals and automation runs." items={[["Ticket SUP-8421 assigned","Support Intake → Missing Rewards","2 min ago"],["Ticket SUP-8420 escalated","Withdrawals → Payments","8 min ago"],["Quality sample recorded","Support Quality · passed","14 min ago"],["SLA warning created","SUP-8418 · critical","21 min ago"]]}/>; }

function SupportList({ title, subtitle, items, action }: { title: string; subtitle: string; items: string[][]; action?: Action }) {
  return <section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-bold">{title}</h2><p className="mt-1 text-xs text-slate-500">{subtitle}</p></div><div className="divide-y">{items.map(([name,detail,state]) => <article key={name} className="flex items-center gap-3 p-5"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-violet-50 text-violet-600"><CheckCircle2 size={16}/></span><div><b className="text-xs">{name}</b><p className="mt-1 text-[10px] text-slate-400">{detail}</p></div><span className={`ml-auto rounded-full px-2 py-1 text-[9px] font-bold ${tone(state)}`}>{state}</span>{action && <button onClick={() => action(`${name} opened for review`)} className="text-xs font-bold text-violet-700">Review</button>}</article>)}</div></section>;
}
